# Copy to .env and fill in. Never commit .env.
# Clerk publishable key (same production instance as medyra.de)
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_xxx
# Backend base URL (the web app's API routes are the single source of truth)
EXPO_PUBLIC_API_URL=https://medyra.de
